module.exports = function(Parse, app, module) {

    var loaderView = require('./view');

    module.$ = (function() {

        var vm = {

        }

        vm.init = function(options) {
            
        }

        return vm;

    }())

}
